# Tools are native host tools
# They are currently skipped from the CMake build as they require special handling
# They can be built separately using their respective build scripts if needed
message(STATUS "Tools subdirectory: native tools skipped in main build")
